# 📚 CourseBookingApp

A full-stack **course booking application** that lets users browse courses and make bookings through a client interface with a dedicated backend API.

This project includes a separate **frontend (client)** and **backend (server)** to make development and deployment modular and scalable.

---

## 🧠 Overview

CourseBookingApp is designed to simulate a simple educational platform where users can:

✔ Browse available courses  
✔ View course details  
✔ Register for courses  
✔ Make and manage course bookings

This project follows a **client-server architecture** with a frontend UI and backend API communicating over HTTP.

---
(images/readmeSS.png)
---

## 🧱 Project Structure

| Folder | Description |
|--------|-------------|
| `client` | Frontend application (likely using a modern JS framework) |
| `server` | Backend API (serves data to client and handles business logic) |

## 🛠️ Tech Stack

**Frontend (client):**
- Vue.js
- Vite
- JavaScript / HTML / CSS

**Backend (server):**
- Node.js
- Express.js
- REST API